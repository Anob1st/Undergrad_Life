#include <stdio.h>

int main() {
    char name[100];
    scanf("%s", name);

    // Print the greeting lines
    printf("Hello, %s!\n", name);
    printf("Welcome to C programming!");

    return 0;
}
